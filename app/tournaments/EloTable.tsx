"use client";
import { useState } from "react";
export default function EloTable({players}:{players:any[]}){const [open,setOpen]=useState(false);const rows=[...players].sort((a,b)=>b.elo-a.elo||a.name.localeCompare(b.name));return <section className="grid"><article className="card wide"><button className="secondary" onClick={()=>setOpen(!open)} aria-expanded={open}>{open?"Hide Elo":"Show Elo"}</button>{open&&<><p className="eyebrow">Tournament ratings</p><h2>Elo</h2><table><thead><tr><th>Player</th><th>Starting</th><th>Current</th><th>Change</th></tr></thead><tbody>{rows.map(p=>{const change=p.elo-p.startingElo;return <tr key={p.id}><td>{p.name}</td><td>{p.startingElo}</td><td>{p.elo}</td><td className={change>0?"gain":change<0?"loss":""}>{change>0?"+":""}{change}</td></tr>})}</tbody></table></>}</article></section>}
